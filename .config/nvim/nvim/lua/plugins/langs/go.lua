-- Base configuration for Go is setup through LazyVim's langs.go extra
-- This setup is mostly for the use of Hugo and other libraries
return {
  {
    "phelipetls/vim-hugo",
    enabled = false,
    opts = {},
  },
}
